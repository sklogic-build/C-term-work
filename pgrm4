
#include <stdio.h>
int main()
{
    int n,sum=0,sign=1,i;
    printf("Enter the range for series: \n");
    scanf("%d",&n);
    for(i=1;i<=n;i++)
    {
        sum+=sum+i*i*sign;
        sign=sign*(-1);
    }
    printf("Sum of series is: %d \n",sum);
    return 0;
}
